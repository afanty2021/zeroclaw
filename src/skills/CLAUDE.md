# Skills Module — 技能管理系统

**模块路径**: `src/skills/`

## 概述

Skills 模块管理用户定义和社区构建的技能，支持加载、审计、安装和安全验证。

## 核心功能

### 1. Skill

技能定义：
```rust
pub struct Skill {
    pub name: String,
    pub description: String,
    pub version: String,
    pub author: Option<String>,
    pub tags: Vec<String>,
    pub tools: Vec<SkillTool>,
    pub prompts: Vec<String>,
    pub location: Option<PathBuf>,
}
```

### 2. SkillTool

技能工具：
```rust
pub struct SkillTool {
    pub name: String,
    pub description: String,
    pub kind: String,      // "shell" | "http" | "script"
    pub command: String,
    pub args: HashMap<String, String>,
}
```

### 3. CLI 命令

- `skills list`: 列出已安装技能
- `skills audit <source>`: 审计技能
- `skills install <source>`: 安装技能
- `skills remove <name>`: 删除技能

### 4. 技能格式

### SKILL.toml
```toml
[skill]
name = "my-skill"
description = "What this skill does"
version = "0.1.0"
author = "your-name"
tags = ["productivity", "automation"]

[[tools]]
name = "my_tool"
description = "What this tool does"
kind = "shell"
command = "echo hello"
```

### SKILL.md
简化格式，仅 Markdown 指令。

### 5. Open Skills

社区技能集成：
- 仓库：`https://github.com/besoeasy/open-skills`
- 同步间隔：7 天
- 环境变量：
  - `ZEROCLAW_OPEN_SKILLS_ENABLED`: 启用开关
  - `ZEROCLAW_OPEN_SKILLS_DIR`: 自定义目录

### 6. 安全审计

审计检查：
- 路径遍历检测
- 符号链接拒绝
- 敏感文件检测
- 文件权限验证

### 7. 技能注入

生成系统提示部分：
- `Full`: 完整指令和工具
- `Compact`: 仅摘要，按需加载

## 安装源支持

### Git 源
```
https://github.com/user/skill.git
git@github.com:user/skill.git
```

### 本地源
```
./path/to/skill
~/local/skill
```

## 存储位置

```
<workspace>/skills/<name>/
├── SKILL.toml  |  SKILL.md
└── (其他资源)
```

## 相关模块

- `src/skillforge/`: 技能自动发现
- `src/tools/`: 工具执行
- `src/agent/`: 编排循环集成


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>